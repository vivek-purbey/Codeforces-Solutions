#include<iostream>
using namespace std;
int main(){
    int n, k; cin>>n>>k;
    int a[n], count=0; for(int i=0;i<n;i++){
        cin>>a[i];
        if(a[i]<5){
            if(5-a[i]>=k) count++;
        }
    }
    if(count>=3) cout<<count/3<<endl;
    else cout<<0<<endl;
    return 0;
}
